//import { groupSelection } from "@/core/domain/states/Selection";

// const safeValues = (keys: string[]) =>
//     keys.reduce((v, k) => {
//         v[k] = { selected: "", index: -1 };
//         return v;
//     }, {} as groupSelection);